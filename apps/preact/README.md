* uses VirtualDOM to  DIFF
* HTML markup via functions `h('div')`
* Component logic (life-cycles and more) in markup render functions
* best DOM markup hydratation (after first Client load re-render only differences)
